using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cake_Stack : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //I think this might be a useless class, but I'm scared to delete as I'm worried the blocks (cake pieces) might not work if I get rid of it!
        //Typing this now, this class is probably what's confusing my code in regards to the triggers. dang it. 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
